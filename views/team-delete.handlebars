
<div class="card text-center">
  <div class="card-body">
    <div class="alert alert-danger">
      <p>Are you sure you want to delete this team <strong>permanently</strong>? Please write the full name of the team below to confirm</p>
    </div>

    <form method="POST" enctype="application/x-www-form-urlencoded">
      <div class="input-group mb-3">
        <input type="text" class="form-control" aria-describedby="delete-btn" name="teamName">
        <div class="input-group-append">
          <button class="btn btn-danger" type="submit" id="delete-btn">DELETE</button>
        </div>
      </div>
    </form>

    {{#if data.error}}
    <p>The name passed is invalid</p>
    {{/if}}

  </div>
</div>